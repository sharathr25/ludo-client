const host = '192.168.1.33:5000' // host address when connected to wifi to check in mobile
// const host = 'localhost:5000'
export const WEBSOCKET_URL = `ws://${host}/socket`

export const API_URL = `http://${host}:5000`
